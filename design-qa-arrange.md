# 单页旅行工作台设计验收

## 参考与实现

- 目标：桌面端将想去清单、当天行程、地图与日期轨合并为一个持续可见的工作台。
- 实现截图：`/tmp/trip-workspace-final.png`
- 验证环境：桌面端 1280px 宽度，东北大环线示例行程。

## 对照结果

| 项目 | 结果 | 说明 |
| --- | --- | --- |
| 单一工作台 | 通过 | 桌面端不再显示想去 / 编排 / 行程切换，四个区域在同一屏协作。 |
| 想去清单 | 通过 | 左侧支持搜索、手动收集、未安排优先；已安排地点仍可拖拽或再次安排到其他日期。 |
| 清单管理 | 通过 | 每个地点均可从想去清单移除；已安排地点移除前会说明行程仍会保留，并支持撤销。 |
| 日程编辑 | 通过 | 中间保留完整当天时间轴、条目详情、交通、花费与备注，不再放置快速添加。 |
| 地图选点 | 通过 | 入口统一收进左侧想去清单；点击后右侧地图直接进入选点状态。 |
| 地点地图联动 | 通过 | 点击想去清单中的地点后，地图切换为地点总览，待安排与已安排地点以不同颜色展示，当前地点带名称高亮。 |
| 日期管理 | 通过 | 底部切换日期、展示地点数与花费，并支持新增、复制当前天。 |
| 日期轨 | 通过 | 日期卡片始终保持单行横向滚动；支持拖到另一张日期卡片上调整顺序。 |
| 总预算 | 通过 | 工作台右上角预算为常驻可点击入口，可查看明细并修改总预算。 |
| 拖放安排 | 通过 | 将想去清单条目拖到中间当前日期会直接创建安排。 |
| 拖放反馈 | 通过 | 拖入中间栏会显示落点高亮与“松开即可安排”提示；成功后新事项和清单条目各有一次轻量入场反馈。 |
| 可调分栏 | 通过 | 想去清单与地图之间分别提供拖动分隔条，宽度会保存到当前浏览器。 |
| 紧凑日程 | 通过 | 工作台内的概览仅保留安排数与当天花费；时间轴和事项卡片均收紧。 |
| 顶部旅程操作 | 通过 | 当前旅行使用轻量下拉菜单切换；新旅行使用目的地与日期表单，不再调用浏览器输入框。 |
| 数据与设置 | 通过 | 设置在工作台覆盖式抽屉中打开，不再跳转和卸载当前规划页面。 |
| 收集入口 | 通过 | 搜索与地图选点并列放在想去清单顶部，减少重复入口和垂直占用。 |
| 小屏策略 | 通过 | 手机端保留现有可读的单列流程，不压缩三栏工作台。 |

## 有意差异

- 配置、备份等低频功能保留在右上角“数据与设置”，不挤占规划工作区。
- 地图跟随当前日期展示；顶部“全程地图”用于查看完整路线。

## 验证

- `npm run build`：通过。
- `npm run lint`：通过；仅保留项目已有的 React lint 警告。
- Playwright：点击底部第 2 天后，中间时间轴与地图切换至该天；想去清单“＋”可安排到当前天；点击清单中的地图按钮后，右侧地图进入选点状态；拖动“调整想去清单宽度”分隔条后，左侧栏成功加宽；“数据与设置”以覆盖式抽屉打开，旅行菜单与创建旅行表单均可正常打开。

## Final result: passed
